## 하네스: 종목 검색 & 차트 기술적 분석 서비스

**목표:** 종목을 검색하고, 검색한 종목의 차트를 기본 지표·차트 패턴·고급 기법(일목균형표/엘리엇파동/변곡점예측)으로 기술적으로 파헤쳐 보는 웹 서비스를 만든다.

**트리거:** 이 서비스 관련 작업 요청 시 `chart-deep-dive-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-11 | 초기 구성 (에이전트 4개: stock-data, technical-analysis, frontend-chart, qa-integration / 스킬 4개 + 오케스트레이터) | 전체 | 초기 하네스 구축 요청 |
| 2026-07-11 | technical-analysis-engine에서 차트 패턴 인식을 chart-pattern-recognition으로 분리 | skills/technical-analysis-engine, skills/chart-pattern-recognition (신규), agents/technical-analysis-agent | 수치 지표(수식)와 구조적 패턴(형태 탐지)은 계산 방식이 달라 별도 스킬이 적절하다는 팀 구성 검토 피드백 |
| 2026-07-11 | chart-pattern-recognition 패턴 목록을 3종(헤드앤숄더/삼각수렴/이중바닥)에서 반전형·지속형·기타 전 범주(삼중바닥, 원형바닥/천장, 깃발형, 쐐기형, 박스권, 컵앤핸들, 확산형, 다이아몬드형)로 확장 | skills/chart-pattern-recognition | "모든 차트 유형이 포함되었는가" 피드백 — 기존 목록이 표준 기술적분석 패턴 분류를 충분히 커버하지 못함 |
| 2026-07-11 | chart-pattern-recognition에 채널, 갭 패턴(돌파/추세/소멸갭+섬반전), V자형 반전, 하모닉 패턴(Gartley/Butterfly/Bat/Crab, references/harmonic-patterns.md로 분리) 추가 | skills/chart-pattern-recognition | "차트패턴유형 폴더에 더 추가할 것이 있는지" 재검토 요청 — Bulkowski 표준 분류 대조 후 4개 카테고리 누락 확인, 전부 추가 |
| 2026-07-11 | 오케스트레이터 파이프라인에 와이어프레임 단계(5) + 사용자 확인 게이트(6) 추가, frontend-chart-agent를 와이어프레임/구현 두 모드로 분리, _workspace 파일 번호 재정렬(03=와이어프레임, 04=구현노트, 05=QA리포트) | skills/chart-deep-dive-orchestrator, agents/frontend-chart-agent, skills/chart-ui-design | "실제구현 전 UX/와이어프레임을 먼저 해야하지 않나" 피드백 — 다중 레이어 시각화(지표/패턴/갭/하모닉/일목균형표 등)로 화면이 산만해지기 쉬워 레이아웃을 코드로 굳히기 전에 승인받는 단계가 필요 |
| 2026-07-12 | MCP(UsStockInfo)/Skill(ichimoku 등)을 런타임 의존성으로 오인하는 설계 오류 수정 — 둘 다 Claude Code 세션 전용 도구이며 배포 서버가 호출 불가함을 명시, 실제 계산은 코드로 직접 구현하도록 정정 | skills/stock-data-fetching, agents/technical-analysis-agent | 실제 구현 착수 직전 아키텍처 검토 중 발견 — 방치 시 배포 후 작동하지 않는 백엔드가 만들어질 뻔함 |
| 2026-07-12 | 초기 구현 완료(백엔드 API+분석엔진+프론트엔드), 이후 미구현 패턴 순차 추가: 원형바닥/천장·V자반전 → 깃발/페넌트·컵앤핸들 → 채널·다이아몬드·섬반전. chart-pattern-recognition 스킬 목록 전부 구현 완료 | lib/analysis/patterns.ts, lib/format.ts, _workspace/02_technical-analysis-agent_output-schema.md | 사용자가 미구현 패턴을 하나씩 지목해 순차 구현 요청 |
| 2026-07-12 | 변곡점예측(inflectionPoints)을 규칙 기반 4종(거래량이상·RSI다이버전스·OBV다이버전스·BB스퀴즈)으로 구현, 프론트 레이어 활성화(마커 렌더링). ML 50%/뉴스 15% 레그는 여전히 범위 밖(문서화됨). 낡은 미구현 문구를 02/04 workspace 문서에서 정정 | lib/analysis/inflection.ts (신규), lib/analysis/index.ts, components/ChartStack.tsx, components/LayerControls.tsx, app/chart/page.tsx | "변곡점예측도 규칙 기반으로 구현해줘" 요청 — 원 스킬 3단 앙상블 중 결정적 코드로 가능한 규칙 레그만 분리 구현 |
| 2026-07-12 | 프론트엔드 미구현 UI 항목 순차 해결: EMA 오버레이 토글, 하모닉 XABCD 연결선+PRZ 강조, 일목 구름 음영(SVG 오버레이 방식, area-series 마스킹 트릭은 캔들을 가리는 부작용으로 폐기) | components/ChartStack.tsx, components/LayerControls.tsx, app/chart/page.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | 사용자가 `_workspace/04` 미구현 목록을 하나씩 지목해 순차 구현 요청 |
| 2026-07-12 | 남은 미구현 UI 2건 마무리: 모바일 RSI/MACD 탭 전환(CSS 미디어쿼리+activeSubTab), 검색창 인라인 자동완성(SearchBox 컴포넌트로 추출해 홈/차트 헤더 재사용). `_workspace/04` 미구현 목록 전부 해결 | components/SearchBox.tsx (신규), components/ChartStack.tsx, app/page.tsx, app/chart/page.tsx, app/globals.css | "둘 다 마저 구현해줘" 요청 — 미구현 목록 마지막 두 항목 |
| 2026-07-13 | 차트 뷰 높이 드래그 조절, 줌인 시 동적 해상도(일봉↔1시간봉) 추가. 이어서 좌측 팬 시 자동 히스토리 로드(기간 버튼은 초기 뷰만, 왼쪽 끝까지 팬해도 빈 공간 없이 과거 데이터 이어붙임) 추가 — 초기 구현이 "전체보기=왼쪽끝"을 "진짜 왼쪽끝 도달"로 오판해 과거 데이터를 무한정 이어붙이는 폭주 버그 있었음, 봉 개수 비율 기반 판정으로 교체해 수정 | components/ChartStack.tsx, lib/sources/yahoo.ts, lib/api.ts, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "차트 크기 조절 + 줌 동적 렌더링" 요청, 이어서 "기간은 초기 선택일 뿐, 팬/줌하면 과거 데이터 자동 표시" 요청 |
| 2026-07-13 | 히스토리 팬 10년 상한(도달 시 빈 공간 없이 정지) 추가, 기본 기간 1Y→3M, RSI/MACD 리사이즈 드래그 핸들 추가(`makeResizeHandler` 팩토리로 일반화), 메인/RSI/MACD 기본 높이 확대(320→560, 110→240). RSI+MACD 동시 토글 시 dev 모드에서만 메인 차트가 찌그러지는 현상 발견 — React StrictMode 이펙트 이중실행 아티팩트로 확인(프로덕션 빌드는 정상), 실버그 아님 | components/ChartStack.tsx, app/chart/page.tsx, _workspace/04_frontend-chart-agent_notes.md | 스크린샷 첨부한 5개 항목 요청(과거 이력 10년 제한, 기본기간 3M, 메인/RSI/MACD 개별 줌, RSI/MACD 높이 조절, 첫 화면 차트 크기) |
| 2026-07-13 | 우측 상단 기간 선택 UI 완전 삭제(항상 10년치 로드로 고정), 메인 차트 초기 뷰는 최근 3개월만 표시하도록 변경(전체 10년을 fitContent로 보여주면 캔들이 찌그러지는 문제라 `setVisibleRange` 방식으로 교체), OHLC 호버 레전드(시가·고가·저가·종가·등락·거래량)와 종목 워터마크 추가. 브라우저 검증 중 리사이즈 시 `fitContent()`가 재호출돼 3개월 기본 뷰가 깨지는 회귀 발견 — 최초 마운트(폭 0→확정) 시에만 refit하도록 조건 수정 | app/chart/page.tsx, components/ChartStack.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "기간 설정 삭제, 메인 캔들 차트 기본 10년 탐색 가능 + 뷰는 3개월 기준, 캔들차트 기본 요소 다 갖추도록" 요청 |
| 2026-07-13 | 패턴 레이어 기본 ON(페이지 로딩 시 3개월 기본 뷰에서 바로 마커 표시)으로 변경, 패턴 목록을 단일선택→체크박스 다중선택(기본 전체선택)으로 전환하고 "전체 선택"/"전체 해제" 버튼 추가. 다중선택으로 바뀌며 의미가 없어진 "패턴 선택 시 그 range로 뷰 강제 이동" 포커스줌과 단일 밴드 하이라이트 오버레이는 제거. 이 과정에서 리사이즈 refit 버그가 변종으로 재발(뷰포트 리사이즈 직후 새로고침 시 폭 0 마운트→3개월 설정 후 ResizeObserver 최초 콜백이 fitContent()로 덮어씀) — 초기 뷰 로직을 `applyDefaultRange()` 함수로 뽑아 마운트/리사이즈 양쪽에서 재사용하도록 통일해 수정 | app/chart/page.tsx, components/ChartStack.tsx, components/LayerControls.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "페이지 로딩 시 첨부 크기로 설정, 그 뷰에서 패턴 표시, 패턴 체크박스 다중선택+전체선택 기능 추가" 요청 |
| 2026-07-13 | 사이드바 패턴 목록 정렬을 confidence순→최근(range.end)순으로 변경("신호요약"의 topPattern은 confidence 기준 유지하도록 분리), 차트 위 패턴 키포인트 마커(top/bottom/rim/head/left-shoulder 등) 영문 텍스트를 한글로 매핑(`patternKindLabel`, 하모닉 X/A/B/C/D는 표준 표기라 유지), 변곡점 마커 `IP`→`변곡`. `timeScale.rightOffset` 4→0으로 바꿔 최신 봉이 우측 가격축에 바로 붙도록 정렬(메인/RSI/MACD 공통 적용) | app/chart/page.tsx, components/ChartStack.tsx, lib/format.ts, _workspace/04_frontend-chart-agent_notes.md | "패턴 감지는 최근순+한글표시, 마지막 날짜를 우측 좌표축에 맞춰줘" 요청 |
| 2026-07-14 | 메인 차트 기본 높이를 고정 px(560)에서 뷰포트 비례(`window.innerHeight*0.62`, 240~1000 clamp)로 변경 — "첨부 크기로 설정" 요청이 스크린샷 캡처 창 크기를 알 수 없어 매번 추측이었던 근본 원인을 구조적으로 해결. 패턴 체크박스를 off→on으로 켤 때 그 패턴 위치로 뷰 이동 + 위치 강조 핑 애니메이션(`.patternpulse`, 메인 빌드 이펙트와 분리된 별도 useEffect라 체크할 때마다 차트 전체가 재생성되지 않음) 추가 | components/ChartStack.tsx, app/chart/page.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "첨부의 뷰크기로 설정, 패턴 체크 시 해당 위치로 이동+알람 애니메이션 추가" 요청 |
| 2026-07-14 | RSI/MACD가 메인과 정렬 안 맞는 버그(마지막 날짜가 우측 축까지 안 붙음) 근본 원인 2건 발견·수정: (1) 날짜 기반 `setVisibleRange`가 `rightOffset:0`+마지막날짜 조합에서 요청보다 훨씬 넓게(3개월 요청 시 실측 13개월) 적용되는 lightweight-charts 동작 확인 — bar 개수 기반 `setVisibleLogicalRange`로 교체(`DEFAULT_VIEW_BARS=63`). (2) RSI/MACD는 워밍업 구간 때문에 자기 차트의 bar 0이 메인보다 밀려있어 logical range를 그대로 복사해도 어긋남 — 워밍업 구간을 `toLineWithGaps()`로 whitespace 포인트를 채워 넣어 bar 인덱스를 메인과 1:1 정렬. 캔버스 픽셀 직접 스캔(색상 매칭으로 마지막 데이터 컬럼 탐지)으로 메인/RSI/MACD 모두 ~99% 일치 확인 | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "실제 창 크기 첨부 확인, 첨부(빨간펜 표시) 수정" 요청 — RSI/MACD 마지막 값이 축까지 안 이어지는 문제 |
| 2026-07-14 | 패턴 위치 핑 애니메이션을 원형 링 확대에서 위/아래로 튀는 화살표(방향은 저점/고점에 따라 ▲/▼, `--up`/`--down` 색)로 교체. 하모닉 전용이던 keyPoints 연결선을 이중/삼중바닥·천장·원형바닥/천장·V자반전·헤드앤숄더 등 모든 다중 봉 패턴으로 확장해 실제 형태(W자, 곡선, V자 등)가 캔들 위에 그려지도록 함 | components/ChartStack.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "위치 애니메이션을 화살표 통통 튀는 걸로, 다중 봉 패턴도 형태를 그려달라" 요청 |
| 2026-07-14 | 위치 핑 화살표에 형광(`--neon: #39ff14`) 테두리(`-webkit-text-stroke`+글로우) 추가, 패턴 형태선을 카테고리색→형광 단색, 얇게→굵게(lineWidth 3), 직선→곡선(`LineType.Curved`)으로 변경. 패턴 기본값을 전체선택→전체해제로 뒤집음(레이어 스위치 자체는 기본 ON 유지, 체크박스만 비움) | components/ChartStack.tsx, app/chart/page.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "화살표 테두리 형광색, 패턴 기본 전체해제, 패턴 선 형광색+굵은 곡선" 요청 |
| 2026-07-14 | 패턴 체크 시 메인 캔들 차트가 순간 커졌다 사라지는 플리커 수정 — `selectedPatterns`가 메인 차트를 통째로 만드는 rebuild 이펙트의 의존성에 들어있어 체크할 때마다 차트 3개(메인/RSI/MACD) 전체를 파괴·재생성하고 있었음(새 차트가 크기 확정 전 한 프레임 그려지는 게 커졌다 사라지는 것처럼 보임). 패턴 마커/형태선 그리기를 `drawPatternShapes()`로 분리해 rebuild 이펙트 밖으로 빼고, `selectedPatterns` 전용의 가벼운 이펙트가 기존 차트 인스턴스를 재사용해 라인만 추가/제거하도록 수정. 캔버스 DOM 노드 identity 비교로 차트가 더 이상 재생성되지 않음을 확인 | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "패턴 체크하면 봉차트가 잠깐 커졌다가 사라지는 현상 해결해줘" 요청 |
| 2026-07-15 | 패턴 목록 높이를 메인 차트 높이에 동기화 + 내부 스크롤(`onMainHeightChange` prop 신설, 페이지 스크롤 9863px→1400px), RSI 기본 ON, 패턴 다중선택을 key 기반 diff 구조로 재설계(새로 체크한 패턴만 라인이 점진적으로 그려지는 애니메이션 + 화살표가 체크 해제 전까지 계속 표시·pan/zoom 따라 위치 재계산), 갭상승/갭하락/일반갭 등 range span이 1~2일뿐인 패턴에서 심하게 발현되던 focus-jump 플리커를 날짜 기반→bar-index 기반 `setVisibleLogicalRange`로 교체해 수정 | components/ChartStack.tsx, app/chart/page.tsx, components/LayerControls.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "패턴 타일 높이 동기화+스크롤, RSI 기본표시, 라인 그리기 애니메이션, 화살표 계속표시, 갭 패턴 플리커 수정" 요청 |
| 2026-07-15 | 패턴 형태선 리빌 애니메이션(속도 500ms→1000ms→3000ms 순차 조정 후) 뚝뚝 끊기는 문제 수정 — keyPoints 개수(보통 2~5개)만큼만 `series.setData()`로 단계적 공개하던 방식이라 점 개수가 곧 애니메이션 프레임 수였음. lightweight-charts 라인 시리즈를 plain SVG `<path>`로 교체(`catmullRomPath` 곡선 변환 + `revealPath`의 `stroke-dashoffset` 트랜지션)해 점 개수와 무관하게 매끄럽게 그려지도록 근본 수정 | components/ChartStack.tsx, app/globals.css, _workspace/04_frontend-chart-agent_notes.md | "순차로 그려지는 애니메이션 속도 2배 느리게" → "3000ms로" → "뚝뚝 끊기는가?" → "고쳐" 요청 |
| 2026-07-15 | 하모닉 패턴(Gartley/Butterfly/Bat/Crab) 점선 스타일 복구 — 리빌 애니메이션이 stroke-dasharray를 점유하면서 걷어냈던 것을, `revealPath()`에 `finalDashArray` 파라미터를 추가해 애니메이션 종료 시점에만 점선 패턴으로 바꿔치기하도록 수정(그리는 동안은 계속 매끄러운 solid reveal 유지) | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "하모닉 패턴도 점선으로 다시 구분해줘" 요청 |
| 2026-07-16 | §20 리빌 애니메이션이 실제로는 전혀 재생되지 않던 회귀 발견·수정 — 새 SVG path에 트랜지션을 같은 tick에 걸면 브라우저가 무시하는 문제를 피하려고 넣은 이중 `requestAnimationFrame`이 이 브라우저 환경에서 아예 실행 안 됨(페이지 비활성/백그라운드 취급 시 rAF 정지, `getComputedStyle`로 `transition:"none"`에 고착된 것 확인). `setTimeout(fn,20)`으로 교체해 신뢰성 있게 스케줄링되도록 수정 | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "발견한 패턴 라인 그려주는 기능이 없어졌다" 리포트 |
| 2026-07-16 | 갭상승/갭하락/일반갭처럼 keyPoints 2개(span 1~2일)뿐인 패턴 체크 시 확대된 봉차트가 0.5초 나타났다 사라지는 버그 수정 — focus-jump 후 뷰가 `ZOOM_BAR_THRESHOLD`(15봉) 밑으로 내려가면 "줌인 시 동적 해상도"가 오작동해 인트라데이 데이터로 자동 전환하며 차트 전체를 rebuild하고 있었음(다중선택 리팩터링 때 빠진 `selectedPattern` 가드가 원인). `patternFocusActiveRef`로 focus-jump 직후 2초간 해상도 전환을 억제하도록 가드 복구 | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "갭하락 선택 시 확대된 봉차트가 0.5초 등장했다 사라지는 현상, 왜 그런가" → "고쳐줘" |
| 2026-07-16 | 위 수정이 실제로는 불충분했음을 재현·발견 — "전체 해제 후 같은 패턴 재체크" 시 여전히 조용히 인트라데이(1시간봉)로 전환되고 있었음. 원인: 2초 타이머 해제가 근본적으로 잘못됨(갭 패턴이 만든 좁은 뷰는 원래 계속 임계값 밑에 머무는 게 정상이라, 타이머 만료 즉시 재발동). 시간 기반 해제를 버리고 사용자의 실제 wheel/mousedown 조작이 있을 때만 억제 해제하도록 재설계, `checkLoadMore()`에도 누락됐던 가드 추가, focus-jump 시 이미 예약된 잔여 zoom 타이머도 명시적으로 clearTimeout | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "수정 안 되었음"(첨부 스크린샷과 동일 시나리오 재현) 리포트 |
| 2026-07-17 | 패턴 focus-jump 확대율을 patten span 비례 padding에서 `DEFAULT_VIEW_BARS`(63바=3개월) 고정폭으로 변경 — 갭 계열처럼 keyPoints 2개뿐인 패턴이 과하게 확대돼 보이던 문제 해결. 데이터 최근 끝부분 근처 패턴은 clamp로 폭이 좁아지는 걸 반대쪽으로 보정해 항상 정확히 63바를 확보하도록 추가 수정 | components/ChartStack.tsx, _workspace/04_frontend-chart-agent_notes.md | "여전히 크게 보임, 3개월이 보이는 뷰 크기로 설정해줘" 요청 |
| 2026-07-17 | 패턴 전체 목록 완전성 재검증(스킬 목록 vs `PATTERN_LABELS` vs 실제 탐지 함수 30종 1:1 대조, 누락 없음 확인) 후, 유일하게 문맥적으로 뭉뚱그려져 있던 갭 세분류를 스킬 정의(위치 기반: 돌파갭/추세갭/소멸갭/일반갭)대로 재구현 — 기존 `gap-up`/`gap-down`/`common-gap`(방향+되메움 여부만 반영)을 폐기하고, 갭 발생 전 20봉 구간의 추세 기울기(정규화)·가격 레인지·거래량으로 갭을 분류하는 `gapContext()` 추가: 횡보 이탈=돌파갭, 추세 중간=추세갭, 거래량 급증 동반=소멸갭(5봉 내 되메움 시 confidence 가산), 그 외=일반갭 | lib/analysis/patterns.ts, lib/format.ts, components/LayerControls.tsx | "갭 세분류(돌파/추세/소멸)도 실제로 구분해주고, 이름도 정리된 이름으로 함께 추가해줘" 요청 |
| 2026-07-17 | 패턴 전체선택은 형광 라인 리빌 애니메이션(3초)이 재생되는데 개별선택은 재생 안 되던 버그 수정 — `drawPatternShapes()`가 `patternRevealedRef`에 해당 key를 애니메이션 시작과 동시에(동기적으로) "완료"로 표시해버려서, 개별선택 시에만 같이 발동하는 포커스점프(해당 위치로 뷰 이동)의 range-change 핸들러가 호출하는 `drawPatternLinePositions()`가 이미 "완료"로 표시된 라인을 즉시 `transition:none`+풀렝스로 스냅시켜 리빌이 시작하기도 전에 끝나버렸던 것(전체선택은 뷰 이동이 없어 이 경합이 발생 안 함). `patternRevealedRef` 등록을 애니메이션 실제 종료 시점(`PATTERN_REVEAL_MS`=3000ms 후)으로 지연시켜 수정 | components/ChartStack.tsx | "패턴 전체선택은 라인 애니메이션 재생되는데 개별선택은 안 되는 이유?" 질문 |
| 2026-07-17 | (1) 패턴 형광 라인/화살표가 가격축 숫자 위로 겹쳐 그려지던 문제 수정 — `.patternlines`/`.patternarrows`가 `inset:0`으로 가격축 거터까지 포함한 패널 전체 폭을 차지하고 있었음(`.patternlines`는 `width:100%`까지 명시돼 있어 동적 `right` 조정이 씹혔던 것도 같이 제거). `main.priceScale("right").width()`로 축 폭을 읽어 `drawPatternLinePositions()`에서 매번 두 레이어의 `right` 오프셋으로 적용, `overflow:hidden`도 추가해 실제로 그 경계에서 잘리도록 함. (2) 가격축 라벨을 완전 원화 금액(`.00` 포함)에서 천원 단위 정수로 축약 — 메인 캔들 차트에만 `localization.priceFormatter`로 `formatAxisPrice()` 적용(원화가 아닌 종목은 그대로, RSI/MACD 서브패널은 별도 차트 인스턴스라 영향 없음). OHLC 레전드 등 기존 원 단위 표시는 그대로 유지 | components/ChartStack.tsx, lib/format.ts, app/globals.css | "가격축 위에 오버레이 겹쳐 그리지 말 것 / 가격축 숫자 .00 빼고 천원 단위로" 요청 |
| 2026-07-17 | 위 (1) 수정이 실은 전혀 동작 안 하고 있었음을 재검증 중 발견 — `priceScale("right").width()`가 이 프리뷰 환경(탭이 `document.visibilityState:"hidden"`으로 고정)에서 항상 0을 반환해, 매번 `right:0px`로 클립이 사실상 무적용 상태였음(그간의 "정상 동작" 확인은 축 경계에서 먼 패턴만 우연히 테스트된 것). `AXIS_WIDTH_FALLBACK`(60px) 하한을 둬 측정값이 0이어도 항상 최소 클립이 걸리도록 수정. 화살표는 중심정렬(`translate(-50%)`)이라 경계에 걸치면 통째로 사라지는 것도 확인돼, `repositionArrows()`에 좌표 clamp(plotWidth - 12px) 추가 | components/ChartStack.tsx | "가격 축 위로 그려지는 걸 축 밑으로 그려달란 것이었다" 재확인 요청 — 축이 캔들 플롯과 나란한 별개 캔버스라 클립=마스킹과 결과가 동일함을 확인 후, 실제 클립이 씹히고 있던 버그 발견 |
| 2026-07-17 | (1) 패턴 체크 시 뷰를 자동으로 이동/확대하던 focus-jump 기능 전체 제거 — `focusPattern` state·effect, `patternFocusActiveRef`와 그에 딸린 zoom/loadMore 가드, wheel/mousedown 리스너까지 관련 인프라 전부 삭제(사용자 뷰는 항상 그대로 유지, 패턴은 현재 보이는 범위에 있으면 라인/화살표만 표시). (2) 골든크로스/데드크로스(MA20×MA60 교차, 결정론적 confidence=1) 신규 패턴 타입 추가 — `lib/analysis/patterns.ts`에 `movingAverageCrosses()` 추가, 카테고리 `cross` 신설(라벨·색상 토큰 `--cross` 포함), keyPoints 2개짜리라 기존 라인 리빌 인프라 그대로 재사용하되 리빌 시간만 1.5초(`CROSS_REVEAL_MS`)로 다르게 적용. 골든크로스는 위쪽 화살표/상승색 표시되도록 `isLow` 판정 정규식에 "golden" 추가. 컵앤핸들/헤드앤숄더/이중바닥은 이미 keyPoints>=2 라인 리빌 경로를 타고 있어 추가 구현 불요(확인만 함) | components/ChartStack.tsx, lib/analysis/patterns.ts, lib/format.ts, app/globals.css, app/chart/page.tsx | "패턴 표시가 뷰를 바꾸지 말 것" + "컵앤핸들/H&S/이중바닥/데드크로스/골든크로스도 라인 애니메이션으로 표시" 요청 |
| 2026-07-17 | 사용자가 "패턴 선택 시 뷰 이동 안 됨" 스크린샷 리포트 중, 진짜 원인이 focus-jump 제거가 아니라 그 전 라운드(가격축 클립)의 회귀였음을 발견 — `.patternlines`에서 `width:100%;height:100%`를 제거하고 `right`만 동적으로 주던 방식이, `<svg>`가 대체 요소(replaced element)라 inset만으로는 크기가 안 잡히고 기본 300x150 고유 크기로 쪼그라들어 있었음(패턴 line은 DOM엔 정상 존재하지만 SVG 박스 밖이라 안 보였던 것). `width/height:100%` 복구하고, 가격축 클립은 박스 리사이즈 대신 `clip-path: inset(0 Npx 0 0)`으로 전환(레이아웃 후 적용이라 대체 요소 크기 버그 영향 없음). 이어서 (1) focus-jump를 완전 삭제 대신 "줌 폭은 유지, 위치만 이동"으로 재구현(현재 `getVisibleLogicalRange()`의 폭을 그대로 재사용, 강제 축소 없어 zoom-adaptive-resolution 가드도 다시 불필요) (2) 가격축 포맷을 천원 단위에서 완전 원 단위 정수로 되돌림(`formatAxisPrice`) | components/ChartStack.tsx, app/globals.css, lib/format.ts, app/chart/page.tsx | "뷰 크기는 그대로 두고 위치만 이동, 가격축은 1원 단위로, 라인이 안 그려짐" 리포트 |
| 2026-07-17 | (1) 한국 종목명이 Yahoo 소스 탓에 영문("Samsung Electronics Co., Ltd.")으로 표시되던 문제 수정 — Naver 검색 API로 6자리 코드→한글명을 역조회하는 `resolveKoreanName()` 추가(`lib/sources/naver.ts`), `/api/search`(Yahoo로만 발견된 KR 결과 보정)와 `lib/sources/ohlcv.ts`의 `loadOhlcv`(KR 시세 응답 name 자체를 한글로 override) 양쪽에 적용. 이 과정에서 `/api/ohlcv/route.ts`가 `lib/sources/ohlcv.ts`(analysis가 이미 쓰던 공용 로직)를 안 쓰고 자체적으로 `fetchKrWithSuffix`를 중복 구현하고 있던 걸 발견 — 그래서 처음엔 이 라우트만 그대로라 수정이 안 먹혔음. 라우트를 공용 `loadOhlcv`/`parseOhlcvParams` 위임으로 리팩터해 중복 제거하며 같이 고침. (2) 폰트를 Space Grotesk(라틴/숫자, next/font/google 셀프호스팅)+Noto Sans KR(한글, next/font/google이 CJK 서브셋 미지원이라 `<link>` 태그로 로드)로 교체 — `--sans` 스택 순서만으로 문자별 자동 폴백(Space Grotesk에 없는 한글 글리프는 자동으로 Noto Sans KR로 넘어감) | app/api/ohlcv/route.ts, lib/sources/ohlcv.ts, lib/sources/naver.ts, app/api/search/route.ts, app/layout.tsx, app/globals.css | "한국주식은 이름/코드 검색+한글표시, 미국주식은 영어+티커" + "한글은 Noto Sans KR, 영문/숫자는 Space Grotesk" 요청 |
| 2026-07-17 | 4건 추가: (1) 메인 캔들 차트에 수평선·추세선 그리기 툴 추가 — 수평선은 lightweight-charts 네이티브 `createPriceLine`(클릭 1회, y좌표→가격 변환), 추세선은 기존 patternlines와 같은 방식의 커스텀 SVG `<line>` 오버레이(클릭 2회, `main.subscribeClick`으로 좌표 수집). 그려진 선은 `horizontalsRef`/`trendsRef`에 보관해 레이어 토글 등으로 인한 잦은 차트 재생성에도 유지(진짜 심볼 변경 시에만 초기화), `지우기` 버튼으로 전부 제거 가능. (2) 거래량을 메인 차트 하단 오버레이에서 메인-RSI 사이 독립 서브패널로 분리(`LayerState.volume`, 리사이즈 핸들·레이어 토글 포함, 기존 RSI/MACD 패턴 그대로 재사용). (3) 첫 화면 헤드라인 영문화. (4) 검색창에 검색 버튼(🔍) 추가, 기존 Enter 제출 로직 재사용 | components/ChartStack.tsx, components/LayerControls.tsx, components/SearchBox.tsx, app/page.tsx, app/chart/page.tsx, app/globals.css | "수평선/추세선 표시 기능, 거래량 메인-RSI 사이 이동, 첫화면 문구 영문화, 검색 버튼 추가" 요청 |
| 2026-07-17 | 첫 화면을 Google 홈페이지 레이아웃 패턴(미니멀 무테두리 헤더, 큰 중앙 워드마크, 필 형태 검색창, 원형 아이콘 단축키)으로 재구성 — Google 실제 로고/개인 계정 정보(첨부 스크린샷에 있던 프로필·Gmail·로그인 기록)는 제외하고 레이아웃 스타일만 차용, 브랜드는 기존 "Chart Deep Dive" 유지. `.searchhero` 세로 중앙 정렬+그라데이션 제거, `.chip`을 사각 카드에서 원형 이니셜 아이콘+라벨로 교체 | app/page.tsx, app/globals.css | 구글 홈페이지 스크린샷 첨부, "첫 페이지를 첨부와 같이 변경해줘" 요청 |
| 2026-07-17 | (1) 검색창 버튼을 아이콘(🔍)에서 "검색" 텍스트 버튼으로 변경, 필 형태 강조 스타일(accent 배경) 적용. (2) 그리기 툴 "지우기"를 "전체 지우기"로 명확화하고, 수평선/추세선 개별 삭제 기능 추가 — `horizontalsRef`/`trendsRef`에 고유 id 부여(`drawingIdRef` 카운터), `priceLineHandlesRef`를 배열에서 `Map<id,IPriceLine>`으로 전환해 특정 하나만 `removePriceLine` 가능하도록 수정. 툴바 밑에 그려진 선 목록(칩+× 버튼)을 새로 렌더링 — 이 목록은 refs 값을 직접 읽으므로 add/remove마다 `drawingsTick` state를 bump해 재렌더 트리거 | components/SearchBox.tsx, components/ChartStack.tsx, app/globals.css | "검색 버튼 명시적으로 추가, 지우기는 전체 지우기로 두고 수평선/추세선 개별 삭제 가능하게" 요청 |
| 2026-07-17 | (1) 개별 수평선/추세선을 메인 캔들 차트 위에 넘버링 표시 — 수평선은 lightweight-charts `title` 옵션에 "수평선 N" 형식으로(축 라벨에 자동 표시), 추세선은 SVG `<g>`로 `<line>`+`<text>` 묶어 중점에 번호 렌더링(`drawUserLines` 확장). 번호는 안정적 id가 아니라 "현재 배열 순서"로 매겨지므로, 하나 삭제 시 나머지가 자동으로 재넘버링됨 — 수평선은 IPriceLine 타이틀을 생성 후 수정할 수 없어 삭제/추가마다 전체를 지우고 배열 순서대로 다시 생성하는 `reapplyHorizontals()`로 통일(개수가 적어 비용 무시 가능). (2) "전체 지우기" 버튼에 클릭 시 pressed 시각효과 추가 — `justCleared` state를 클릭 시 true로 잠깐(220ms) 두어 `.pressed` 클래스(빨간 배경+스케일 축소)를 플래시 | components/ChartStack.tsx, app/globals.css | "개별 추세선/수평선 넘버링 메인 차트에 표시, 전체 지우기 버튼에 pressed 효과 추가" 요청 |
| 2026-07-17 | 사용자 첨부 디자인 스펙 2건(Voltagent/다크, Airtable/라이트) 그대로 테마 토큰에 적용 — 다크: 캔버스 #101010, 단일 accent 그린 #00d992, 카드 radius 8px/버튼 6px. 라이트: 캔버스 #ffffff, ink 기반 accent #181d26(근흑, "검정이 곧 primary"), 카드 10px/버튼 12px. 폰트를 두 스펙 모두 지정한 Inter로 교체(Space Grotesk 대체, next/font/google 셀프호스팅 유지, 한글은 기존처럼 Noto Sans KR 폴백). `--radius-btn` 신규 토큰 추가해 버튼류만 별도 스케일 적용. 적용 직후 대비 버그 발견·수정: `--accent-ink`가 원래 "약한 accent-soft 배지 위 텍스트"와 "진한 accent 배경 위 텍스트" 두 문맥에 혼용되고 있었는데, 새 라이트 테마에서 accent=근흑/accent-ink=흰색이 되며 전자 문맥(옅은 배경+흰 글자)이 사실상 안 보이게 됨(신호요약 버튼, 첫 화면 최근검색 원형 이니셜, 인라인 링크) — 해당 3곳만 `--accent-ink`→`--accent`로 교체해 옅은 배경에도 늘 진한 텍스트가 오도록 수정. 캔들 상승/하락색과 패턴 카테고리색은 두 스펙 다 다루지 않는 금융 차트 고유 색이라 그대로 유지 | app/globals.css, app/layout.tsx | Voltagent/Airtable 디자인 스펙 md 첨부, "첨부1은 다크모드, 첨부2는 라이트 모드에 적용" 요청 |
| 2026-07-17 | 4건 추가: (1) 한국 종목명이 Yahoo 소스 탓에 영문으로 표시되던 문제 재확인·정상 동작 검증. (2) 차트 페이지 헤더 뒤로가기 버튼 "◀ 검색" → "Chart Deep Dive"로 변경(홈 이동 동작은 유지). (3) "표시할 시세 데이터가 없습니다" 매번 표시 리포트 조사 — 재현 실패(최근검색 클릭/검색창+검색버튼 KR·US 전부 정상), `candles.length===0` 분기 조건만 확인해 안내. (4) 차트 헤더 검색바 위치를 브랜드-왼쪽/토글-오른쪽 사이에서 폭만 늘어나던 좌측 정렬 방식에서, `.appbar__center` 래퍼(flex:1 + justify-content:center)로 감싸 진짜 가운데 정렬로 변경(기본 상태·검색 활성 상태 둘 다) | app/chart/page.tsx, app/globals.css | "◀검색→Chart Deep Dive로", "시세없음 메시지 왜 매번뜨나", "검색바 기본 위치를 첨부 위치로" 요청 |
| 2026-07-17 | "표시할 시세 데이터가 없습니다" 재현 성공(스크린샷)한 사용자 리포트로 재조사, 실제 근본 원인 발견·수정 — 심볼 변경 시 fetch effect가 `setLoading(true)`만 하고 `ohlcv`/`analysis`는 그대로 둬서, 새 심볼 렌더 중 `loading&&!ohlcv` 조건이 (이전 심볼의 ohlcv가 아직 살아있어) false로 걸려 로딩 스켈레톤을 건너뛰고 있었음 — 경합 타이밍에 따라 EmptyState 분기까지 새는 경로였음. `fetchYahooOhlcv`는 성공 시 항상 실제 캔들을 채우고 실패 시 null만 반환하므로(빈 배열의 성공 응답은 애초에 안 만듦) EmptyState 자체는 사실상 도달 불가 분기였고, 진짜 증상은 "로딩 표시가 새 데이터 준비 전에 사라짐"이었음. fetch effect 시작 시 `setOhlcv(null); setAnalysis(null);` 추가해 심볼 전환마다 최초 마운트와 동일하게 로딩 스켈레톤("시세·분석을 불러오는 중…", 기존 컴포넌트 재사용)이 항상 뜨도록 수정 — 새 문구 추가 불요 | app/chart/page.tsx | 빨간펜 스크린샷 첨부, "새 종목 검색 시 이 메시지 왜 나오나, 필요하면 로딩 문구로 바꿔달라" 요청 |
| 2026-07-18 | 위 수정으로도 해결 안 돼 NVDA 스크린샷 재리포트, 진짜 근본 원인 발견·수정 — dev 모드 React 18 StrictMode가 fetch effect를 이중 실행해, 첫 번째(가짜) 실행의 요청이 자기 cleanup으로 거의 즉시 abort되며 reject되는데, `.catch()`는 `AbortError`를 정상 무시해도 뒤이은 `.finally(() => setLoading(false))`는 reject 사유와 무관하게 무조건 실행돼 실제(두 번째) 요청이 아직 진행 중인데도 `loading=false`로 뒤집어버림 — 그 결과 `loading=false, ohlcv=null` 렌더가 EmptyState로 새는 경로였음(로컬 fetch가 빨라 자가교정되며 재현이 들쭉날쭉했던 이유). `.finally()` 콜백에 `if (!ctrl.signal.aborted) setLoading(false)` 가드 추가해 진짜 요청만 loading을 내리도록 수정. NVDA/AAPL/TSLA/SK하이닉스로 25ms 간격 폴링 검증, EmptyState 텍스트 재현 안 됨 확인 | app/chart/page.tsx | "이 화면이 왜 자꾸 나오는가?" NVDA 빨간펜 스크린샷 재리포트 |
| 2026-07-18 | GitHub(drbrookskim/chartdeepdive)에 초기 push 후 Vercel(Hobby, 서버리스)로 라이브 배포 — 프로젝트명·도메인을 chartdeepdive로 통일하는 과정에서 로컬 폴더 리네임 왕복 중 `.git`/`.vercel`/`.gitignore` 유실(재작성으로 복구, 실제 코드 손실은 없었음 — 이미 초기 커밋에 그날 작업 다 포함), Vercel project rename 시 중복 프로젝트 생성 삭제, 구 도메인(chart-deep-dive.vercel.app)이 배포마다 자동 부활하는 현상을 대시보드에서 도메인 완전 삭제로 해결, SSO 배포보호가 새 도메인을 막던 것도 비활성화. `chartdeepdive.vercel.app`은 Vercel이 자동추적하는 공식 도메인이 아니라 매 배포 후 `vercel alias set`으로 수동 재연결 필요(빈도 낮아 GitHub Action 자동화는 보류) | (신규: .git, .vercel) | "push해서 live 상태로 변경해줘" 요청 |
| 2026-07-18 | 첫 화면 "Dive into the Chart" 타이틀/검색바 수직 위치를 화면 중앙→세로 1/3 지점(하단 기준 2/3)으로 조정(`padding-top: calc(뷰포트높이 * 0.33)`) | app/globals.css | "타이틀 위치를 하단에서 2/3 지점으로" 요청 |
| 2026-07-18 | GA4(G-7TCC1MQPYP) 연결(`next/script` afterInteractive) + Google OAuth 로그인 게이트 추가 — Auth.js(next-auth v5)+Google Provider, JWT 세션(DB 없음), `middleware.ts`로 `/chart`·`/api/ohlcv`·`/api/analysis`·`/api/search` 보호(미로그인 시 홈으로 리다이렉트), 홈은 항상 노출하되 검색 UI는 로그인해야 표시. Google Cloud Console OAuth 클라이언트 발급 및 승인된 리디렉션 URI 등록 과정에서 `redirect_uri_mismatch` 2회 발생 — 1차는 미등록, 2차는 삭제했던 구 도메인(chart-deep-dive.vercel.app)이 배포마다 부활하며 그 URI로 재현된 것으로 확인, 도메인을 대시보드에서 완전 삭제해 해결. 로그인 버튼을 구글 공식 버튼 스타일(4색 G 로고+필 형태)로, 로그인 후에는 아바타+이름 칩(클릭 시 로그아웃)으로 교체 | auth.ts, middleware.ts, app/api/auth/[...nextauth]/route.ts, components/Providers.tsx, app/layout.tsx, app/page.tsx, app/globals.css | "구글 계정 로그인 기능 추가" + "로그인 전/후 버튼을 첨부 스타일로" 요청 |
| 2026-07-18 | 수평선/추세선 드래그 편집 기능 추가 — 그리기 도구 비활성 상태에서 기존 선 위 마우스다운 시 히트테스트(허용오차 8px) 후 드래그로 위치 조정. 수평선은 `IPriceLine.applyOptions({price})`로 즉시 갱신(재생성 없음), 추세선은 클릭한 끝점(p1/p2)만 갱신 후 `drawUserLines()` 재호출. 이어서 실수로 그은 선을 바로 편집할 수 없도록 기본 잠금(실선) 상태로 바꾸고, 롱프레스(500ms, 이동 4px 이내)해야 점선으로 풀리며 편집 가능하게, 편집 중인(점선) 선을 드래그 없이 다시 클릭하면 실선으로 잠기며 편집완료로 표시. 커서도 상태 연동(잠금=pointer, 편집중=ns-resize/move) | components/ChartStack.tsx | "수평선과 추세선을 편집 가능하도록 변경" → "롱프레스하면 점선표시, 다시 선택하면 실선(편집완료)" 요청 |
| 2026-07-18 | 메인 캔들 차트 줌/팬 방식 제약 — 줌은 마우스 휠(`handleScale.mouseWheel`)로만 가능하도록 하고 핀치·가격축 드래그 확대·시간축 드래그 확대(`axisPressedMouseMove`) 전부 비활성화, 팬은 좌우(시간축) 드래그만 남기고 가격축 세로 드래그로 인한 리스케일도 차단 | components/ChartStack.tsx | "줌인/줌아웃은 마우스 스크롤만, 이동은 좌우로만" 요청 |
